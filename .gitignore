# Heavy source mirror (raw HTTrack source, not part of the rebuilt site)
# Lives at Projects/projects/_rhine-build-source/ (outside this repo) since this
# site now runs fully independently of the mirror.

# Derived/cache folders
_preview/
.netlify/
node_modules/

# Build outputs that are regenerated by _build/*.js