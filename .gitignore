# The site is fully self-contained: the committed output is the source of truth
# and the rebuild runs in place (no external source folder required).

# Derived/cache folders
_preview/
.netlify/
node_modules/

# Build outputs that are regenerated by _build/*.js
.vercel

# Secrets / local env (never commit)
.env
.env.*
!.env.example
